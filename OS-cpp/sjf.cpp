#include<bits/stdc++.h>
using namespace std;
int main() {
	cout << "enter the no of processes : ";
	int n;
	cin >> n;
	vector<int> pid(n), at(n), bt(n), ct(n), tat(n), wt(n), f(n, 0), k(n);
	int st = 0, tot = 0;
	float avgwt = 0 , avgta = 0

	                          for (int i = 0; i < n; ++i) cin >> at[i];
	for (int j = 0; j < n; ++j) cin >> bt[j];

	k = bt;

	while (true) {
		int min = INT_MIN;
		int c = n;

	}

}